- name: setup template
  ansible.builtin.template:
    src: pipeline.xml
    des: /tmp/pipeline.xml

- name: Create a jenkins job using basic authentication
  community.general.jenkins_job:
    config: "{{ lookup('file', '/tmp/pipeline.xml') }}"
    name: test
    password: sam123
    url: http://54.208.48.253:8080
    user: sam